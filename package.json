{
  "name": "weather",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://peterheinum.github.io/weather",
  "dependencies": {
    "materialize-css": "^1.0.0",
    "react": "^16.8.3",
    "react-dom": "^16.8.3",
    "react-scripts": "2.1.5",
    "react-window-size": "^1.2.2",
    "recharts": "^1.5.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "npm run remotePublish && gh-pages -d build",
    "remotePublish": "git remote set-url origin https://github.com/peterheinum/weather"

  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "gh-pages": "^2.0.1"
  }
}
